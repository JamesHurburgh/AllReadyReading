define(["jquery"],
    function($) {
        getSetNames = function(){

        };

        getSet = function(setName){
            var set = null;

            return set;
        };

        getWordListNames = function(setName){
            var wordListNames = new []();

            return wordListNames;
        };

        getWordList = function(setName, wordListName){
            var wordList = null;

            return wordList;
        };
    }
);
